# NOTE: you must create an .xcode.env.local file with the static path to your node binary, to prevent the error "Command PhaseScriptExecution failed with a nonzero exit code"
# e.g. export NODE_BINARY=/Users/holmes/.nvm/versions/node/v18.20.4/bin/node
export NODE_BINARY=$(which node)
