import React, { FC } from 'react'
import { View } from 'react-native'
import { Typography } from '../../components/Typography/Typography.component'

export const ChannelList: FC = () => {
  return (
    <View style={{ flex: 1 }}>
      <Typography fontSize={14}>todo</Typography>
    </View>
  )
}
