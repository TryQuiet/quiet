/* eslint-disable */

// Lerna/Nx can invoke this test script with NODE_ENV=development. React Native's
// Animated test fallbacks key off process.env.NODE_ENV === 'test'.
process.env.NODE_ENV = 'test'
