/** An initially selected P2P attempt gets a separate Tor startup budget before its handshake deadline. */
export const ADMISSION_TOR_BOOTSTRAP_TIMEOUT_MS = 5 * 60_000
export const ADMISSION_DRAIN_TIMEOUT_MS = 30_000
