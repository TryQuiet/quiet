import React, { ReactElement } from 'react';
interface WindowWrapperProps {
    children: ReactElement;
    className?: string;
}
export declare const WindowWrapper: React.FC<WindowWrapperProps>;
export default WindowWrapper;
