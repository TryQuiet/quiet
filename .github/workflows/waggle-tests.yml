name: Waggle unit tests

on:
  pull_request:
    paths:
      - packages/waggle/**

jobs:
  unit-tests:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-20.04, macos-latest, windows-latest]

    steps:
      - name: "Print OS"
        run: echo ${{ matrix.os }}
      
      - uses: actions/checkout@v2

      - name: "Setup environment"
        uses: ./.github/actions/setup-env

      - name: "Unit tests"
        run: lerna run test --scope waggle --stream
