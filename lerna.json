{
  "packages": [
    "packages/*"
  ],
  "version": "independent",
  "command": { 
    "version": {
      "allowBranch": ["master", "fix/restore-updater"],
      "conventionalCommits": true,
      "createRelease": "github"
    }
  }
}
