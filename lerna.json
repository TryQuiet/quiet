{
  "packages": [
    "packages/*"
  ],
  "version": "independent",
  "command": { 
    "version": {
      "allowBranch": ["master", "fix/change-app-icon"],
      "conventionalCommits": true,
      "createRelease": "github"
    }
  }
}
